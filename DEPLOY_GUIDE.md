# 🚀 Інструкція з розгортання CourseAI

## Крок 1: Підготовка (5 хвилин)

### 1.1 Створіть акаунт на GitHub
1. Перейдіть на [github.com](https://github.com)
2. Натисніть **Sign up**
3. Введіть email, пароль, username
4. Підтвердіть email

### 1.2 Створіть репозиторій
1. На головній сторінці GitHub натисніть зелену кнопку **New** (або **+** → **New repository**)
2. Назва: `course-generator`
3. Виберіть **Public**
4. Натисніть **Create repository**

---

## Крок 2: Завантаження коду (5 хвилин)

### Варіант А: Через веб-інтерфейс GitHub (найпростіше)

1. У вашому новому репозиторії натисніть **uploading an existing file**
2. Перетягніть всі файли з папки `course-generator`:
   - `index.html`
   - `package.json`
   - `vite.config.js`
   - Папку `src` з файлами `App.jsx`, `main.jsx`, `index.css`
3. Натисніть **Commit changes**

### Варіант Б: Через GitHub Desktop (зручніше для майбутнього)

1. Завантажте [GitHub Desktop](https://desktop.github.com/)
2. Увійдіть у свій акаунт
3. Clone ваш репозиторій
4. Скопіюйте файли в папку репозиторію
5. Напишіть commit message і натисніть **Commit to main**
6. Натисніть **Push origin**

---

## Крок 3: Розгортання на Vercel (3 хвилини)

1. Перейдіть на [vercel.com](https://vercel.com)
2. Натисніть **Sign Up** → **Continue with GitHub**
3. Дозвольте Vercel доступ до GitHub
4. На Dashboard натисніть **Add New** → **Project**
5. Знайдіть `course-generator` у списку → **Import**
6. Налаштування залиште за замовчуванням
7. Натисніть **Deploy**

⏳ Зачекайте 1-2 хвилини...

✅ **Готово!** Ваш сайт доступний за адресою типу:
```
https://course-generator-ваш-username.vercel.app
```

---

## Крок 4: Підключення до n8n

### 4.1 Створіть Webhook у n8n
1. У n8n створіть новий workflow
2. Додайте ноду **Webhook**
3. Налаштуйте:
   - HTTP Method: `POST`
   - Path: `course-generator`
4. Скопіюйте **Production URL** (виглядає як `https://your-n8n.com/webhook/course-generator`)

### 4.2 Оновіть URL у коді
1. Відкрийте файл `src/App.jsx`
2. Знайдіть рядок:
   ```javascript
   const WEBHOOK_URL = 'YOUR_N8N_WEBHOOK_URL';
   ```
3. Замініть на ваш реальний URL:
   ```javascript
   const WEBHOOK_URL = 'https://your-n8n.com/webhook/course-generator';
   ```
4. Commit і push зміни
5. Vercel автоматично перезбудує сайт

---

## 🎨 Кастомний домен (опціонально)

Якщо хочете свій домен (наприклад, `coursegen.com`):

1. Купіть домен на [Namecheap](https://namecheap.com) або [GoDaddy](https://godaddy.com) (~$10-15/рік)
2. У Vercel: **Settings** → **Domains** → **Add**
3. Введіть ваш домен
4. Vercel покаже DNS-записи, які потрібно додати
5. У налаштуваннях домену (Namecheap/GoDaddy) додайте ці записи
6. Зачекайте 10-30 хвилин

---

## 🔧 Корисні команди (для локальної розробки)

Якщо хочете запустити локально для тестування:

```bash
# Встановіть Node.js з https://nodejs.org (версія LTS)

# У папці проекту виконайте:
npm install
npm run dev

# Відкрийте http://localhost:5173
```

---

## ❓ Можливі проблеми

### "Vercel не бачить репозиторій"
→ Перевірте, що репозиторій **Public** або надайте Vercel доступ до приватних репозиторіїв

### "Помилка при build"
→ Перевірте, що всі файли завантажені коректно, особливо папка `src`

### "Форма не відправляється"
→ Перевірте, що WEBHOOK_URL правильний і n8n workflow активний

---

## 📞 Підтримка

Якщо щось не працює - напишіть мені, і я допоможу розібратися!
